kind: Service
apiVersion: v1
metadata:
  name: ui
  namespace: vfde-inv-var-demo
  labels:
    app: vfde-inv-var
    tier: ui
spec:
  ports:
    - name: app
      protocol: TCP
      port: 80
      targetPort: http
      nodePort: 
  selector:
    app: vfde-inv-var
    tier: ui
  clusterIP:
  type: NodePort
  sessionAffinity: None
